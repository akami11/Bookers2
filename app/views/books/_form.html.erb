  <div class="new-comment">
    <%= form_with(model:[@book, @book_comment]) do |f| %>
      <%= f.text_area :comment, class: "w-100",placeholder: "コメントをここに" %>
      <%= f.submit "送信" %>
    <% end %>
  </div>